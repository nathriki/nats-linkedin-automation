# Design: PH Corporate-Worker LinkedIn Storytelling Pipeline

Generated by /office-hours on 2026-09-01
Branch: main
Repo: nathriki/nats-linkedin-automation
Status: APPROVED
Mode: Builder

## Problem Statement

Build an automated pipeline that finds trending Philippine news/issues relevant to corporate and office workers (labor policy, wages, RTO mandates, contractualization, government actions affecting workers — broader than "corruption" alone) and turns them into storytelling/personal-angle LinkedIn posts, aimed first at Filipino corporate/BPO employees.

## What Makes This Cool

A voice that never misses a news cycle on issues Filipino office workers actually feel day-to-day, told as relatable stories instead of generic hot-takes — differentiated from existing PH commentary accounts by being automated and consistent rather than manual and sporadic.

## Constraints

- Storytelling/personal-angle format was chosen specifically for engagement, but a bot has no genuine personal experience to draw on.
- LinkedIn's 2026 algorithm actively suppresses generic/templated AI content and rewards first-person authenticity signals and specific data (source: web research, see Cross-Model Perspective note below — no cross-model run this session, this came from Phase 2.75 landscape search).
- Philippine cyber libel carries a penalty one degree higher than ordinary libel specifically because it's committed via a "computer system" (social media qualifies). "Opinion framing" is not a reliable defense against statements that imply false facts about identifiable people or companies.
- LinkedIn's `w_member_social` posting scope (posting to your OWN profile, per Premise 6) is an Open Permission — self-serve and instant, no app review required. This is NOT the same as `w_organization_social` (posting to a company/org PAGE), which does require weeks-to-months Marketing Developer Platform partner review. Corrected during /plan-eng-review after a prior draft of this doc conflated the two (likely carried over from a different, org-page project) — verify against the LinkedIn developer console before building, but treat this as a low-risk item, not a go/no-go blocker.
- Scraping PH news sites for source content carries its own copyright/ToS exposure, separate from the cyber-libel risk on the posting side. This got one sentence and no follow-through in the original review pass — see the corresponding Open Question and Next Step, added after outside-voice review caught the asymmetry with how thoroughly the libel risk was treated. (plan-eng-review, outside voice finding 8)
- Greenfield repo — no existing code to reuse. NATS is the intended backbone (per repo name and user intent); running a NATS+JetStream broker for a single-user, daily-cadence workload is real ops overhead, accepted here because the pipeline stages (source, score, draft, classify, post) are expected to grow independently (more sources, other platforms) rather than staying single-purpose.
- "Distribution" = posting to the user's own LinkedIn account, not shipping a product to other users.
- Secrets (LinkedIn OAuth token, Telegram bot token, LLM API key) live in environment variables loaded from a gitignored `.env` file, never in NATS message payloads, logs, or committed config. (plan-eng-review, architecture issue 1)
- NATS JetStream is at-least-once delivery: if the poster posts to LinkedIn successfully but crashes/disconnects before acking, the message redelivers and the SAME story posts twice. See the dedup-key requirement in Next Steps. (plan-eng-review, architecture issue 3)
- **Windows/UTF-8 requirement for any Gemini-calling process (drafter, risk-classifier):** on this machine, `locale.getpreferredencoding()` defaults to `cp1252`, not UTF-8. Non-ASCII characters in Gemini's response (an em-dash, in the confirmed repro) silently became the U+FFFD replacement character before we ever touched the string — confirmed via an isolated test comparing the same call with and without `PYTHONUTF8=1`. `.env`/python-dotenv CANNOT fix this: Python decides its UTF-8 mode at interpreter startup, before any application code (including `.env` loading) runs. Fixed by persisting `PYTHONUTF8=1` as a Windows user environment variable (`setx PYTHONUTF8 1`) — but that only takes effect in NEW terminal/process sessions, not the one that set it, and it must also be set for whatever runs this in production later (Task Scheduler, cron equivalent) — carried into Next Steps below. Confirmed NOT a systemic issue for the whole pipeline: the scraper's `requests`-based RSS fetch was checked against all 31 stored `news.raw` items and found clean — this is specific to the Gemini SDK's response handling (`google-genai`/httpx2) on this Windows locale, not a general Python-on-Windows problem.
- **Gemini free-tier quota is per-model and can be a hard daily cap, not just per-minute:** `gemini-3.5-flash`'s free tier is capped at 20 requests/DAY total (`GenerateRequestsPerDayPerProjectPerModel-FreeTier`, discovered via a live 429 error during the Next Step 8 eval, not from docs — the docs page didn't expose the actual numbers). Switched the drafter and risk-classifier to `gemini-3.5-flash-lite` for a much higher daily quota, confirmed as a real current model ID via `client.models.list()`. Real production steady-state (2-6 calls/day) would have technically fit under the old 20/day cap, but left no headroom for retries, redrafts, or testing.
- **Long-running processes on this Windows machine need PowerShell `Start-Process`, not Bash `&`/`disown`.** Discovered during Next Step 9 live testing: NATS silently died at least twice mid-session when started via Bash background jobs, and a stale bot process from one such death kept retrying invisibly, later colliding with a fresh instance over a JetStream durable-consumer binding. Git Bash/MSYS's job-control emulation on Windows does not reliably detach a background process from the calling tool-call's lifecycle. Fix: `Start-Process -WindowStyle Hidden -RedirectStandardOutput/-RedirectStandardError` for NATS and the approval bot both. Applies to any future long-running process on this machine (e.g. the poster, once it's a persistent service rather than a periodic script).

## Premises

1. Content scope is trending PH news/issues relevant to corporate/office workers — broader than corruption alone, narrower than general news.
2. Format is storytelling/personal-angle posts, anchored in a disclosed composite worker archetype — never a fabricated "I personally experienced this" claim presented as genuine, and never a real person's story (restricted to composite archetypes only during /plan-eng-review, code quality issue 2 — this removes the Data Privacy Act 2012 compliance question entirely rather than deferring it).
3. Primary audience is Filipino corporate/BPO employees.
4. The pipeline is automated end-to-end except a review gate for any post naming a specific official/company or asserting a factual corruption claim.
5. NATS is the backbone connecting pipeline stages (scraper → scorer → drafter → risk-classifier → poster). The risk-classifier publishes directly to `post.approved` or `post.pending_review` based on its own verdict — no separate router service (folded during /plan-eng-review, architecture issue 2).
6. No distribution/packaging concerns beyond posting to the user's own LinkedIn account.

Rejected during session: fully-automated-with-zero-review (originally proposed) — rejected after landscape research showed PH cyber libel risk and LinkedIn's authenticity-scoring both cut against posting unreviewed, fabricated-personal-angle content about corruption/government actions.

## Approaches Considered

### Approach A: Minimal script pipeline (no NATS)
Single scheduled script: fetch news → LLM draft → regex-based risk check → Telegram approval for risky posts → auto-post the rest. Fastest to ship (S effort), but skips NATS entirely, contradicting the stated intent, and would need a rewrite to decouple stages later.

### Approach B: NATS-native pipeline (recommended, chosen)
Small worker processes communicating over NATS subjects: scraper → scorer (cheap heuristic, no LLM, ranks all candidates) → drafter + risk-classifier (LLM, run only on the top 1-3 scored candidates; classifier publishes directly to `post.approved` or `post.pending_review`) → Telegram/Slack approval bot → poster. JetStream persists every draft and decision, giving an audit trail that matters given the libel exposure. M-L effort, each stage independently testable/swappable.

### Approach C: Editorial-desk model (every post human-curated)
Same NATS backbone as B, but every draft — not just flagged ones — goes into a daily digest for the user to pick/edit/approve. Lowest legal risk and best authenticity (a human touches every story), but not "fully automated" and throughput is capped by review time.

## Recommended Approach

**Approach B: NATS-native pipeline**, chosen by the user. Matches the project's stated intent (NATS backbone), keeps the pipeline mostly automated while gating only the legally risky subset (named officials/companies, factual corruption claims) through human review, and gives each stage (source, score, draft, classify, post) room to grow independently — e.g. adding new news sources or cross-posting to another platform later without a rewrite. Reaffirmed during /plan-eng-review after both the internal Step 0 scope challenge and an independent outside-voice review raised the same "this could be simpler" concern — the user weighed it twice with full context and kept the per-stage independence over solo-deployment simplicity both times. (outside voice tension 3)

## Open Questions

- **Must resolve before the subject schema is frozen** (Next Step 2 depends on this, not the other way around — sequencing corrected after outside-voice review): which news sources feed the scraper (PH news RSS feeds, X/Twitter trends, Reddit r/Philippines, Google Trends PH, PSA/DOLE releases). RSS, a Twitter/X API, Reddit, and Google Trends all produce structurally different payloads, so the source(s) must be picked before `news.raw`'s shape can be fixed. (plan-eng-review, outside voice finding 4)
- **Must resolve before scraping any source**: for each candidate source, check its ToS/robots.txt for redistribution terms; prefer sources with an official RSS feed sanctioned for this kind of reuse, and avoid full-text reproduction (link back + short excerpt) rather than assuming scraping is fine because the libel risk got more attention. (plan-eng-review, outside voice finding 8)
- **Must resolve before the drafter is built**: no Next Step or Task currently exists for the "which LLM + what concrete mechanism enforces composite-archetype framing" question below, even though it's flagged as load-bearing — see Next Steps for the now-added step. (plan-eng-review, outside voice finding 5)
- What does the scorer actually score for — relevance to the target audience, general engagement potential, or both? Now specified as a cheap heuristic/keyword pass (no LLM call) that ranks all candidates so only the top 1-3 go through the expensive drafter+classifier LLM calls (plan-eng-review, performance issue 1) — exact scoring signal still undefined.
- **Must resolve before the drafter is built** (load-bearing for the doc's central legal/authenticity argument): which LLM powers the drafter, and what concrete mechanism enforces the "disclosed composite archetype" framing in the prompt — this is the thing standing between the pipeline and fabricated first-person claims, so it can't stay a loose prompt instruction.
- **Must resolve before the poster service goes live** (load-bearing for the doc's libel-safety argument): exact risk-classifier rule set (keyword list, named-entity detection, or LLM-based classification), and — critically — what happens on a false negative, i.e. a risky post the classifier misses and auto-posts. The classifier is the single gate protecting against the cyber-libel exposure described in Constraints; its reliability is not yet a solved problem.
- **Must resolve before the poster service goes live**: define a dedup key (e.g. source URL hash + publish date) in the message payload, and require the poster to check it against a small local store of already-posted IDs before posting — closes the JetStream at-least-once double-post risk in Constraints. (plan-eng-review, architecture issue 3)
- **Must resolve before the poster service goes live**: incident-response process if a published post turns out risky (classifier false negative, or a fact later disproven) — manual deletion vs. a documented retraction post, and whether to notify anyone. Elevated from a non-blocking Reviewer Concern to match the classifier-reliability gate. (plan-eng-review, code quality issue 1)
- **Must resolve before the poster service goes live**: kill-switch definition — what counts as "repeated misses" (e.g. 2 risky posts auto-published within 24h) and the automatic response (pause the poster's subscription, alert the user). Elevated from a non-blocking Reviewer Concern to match the classifier-reliability gate. (plan-eng-review, code quality issue 1)
- **Must resolve before the poster service goes live**: build a held-out eval set of 15-20 labeled examples (mix of safe/risky news items) and require the risk-classifier to hit a defined false-negative rate on them, plus a smaller eval for the drafter's archetype-disclosure adherence — gives the classifier-reliability gate an actual measurable bar. (plan-eng-review, test gap 1)
- ~~Where does the Telegram/Slack approval bot run, and who has approval rights~~ — RESOLVED (Next Step 9): Telegram (not Slack), a single `TELEGRAM_CHAT_ID` — just the user, per the original "just the user" assumption. Runs as a persistent process (not periodic like the other stages) on whatever machine the user leaves running; no multi-approver or role concept.
- Approval timeout: intentionally none — a pending-review post sits in `post.pending_review` indefinitely until reviewed (safe by default, since nothing risky auto-publishes). Optionally add a daily digest reminder if the pending queue isn't empty. (plan-eng-review, test gap 2 — documented as intended behavior, not a gap)
- Operational edge cases not yet designed for: zero-news days (does the pipeline skip a cycle, or is there a fallback content type?), and LinkedIn's own post-frequency/rate limits.
- ~~Must investigate before building the drafter/classifier: gate-collision rate~~ — RESOLVED (Next Step 0a): 0-22% of a real 9-story sample would trigger the gate. Not a structural collision; the drafter needs to default to worker-experience framing over political-actor framing (folded into Next Step 6).
- ~~Must investigate before building the full pipeline: disclosure vs. authenticity~~ — RESOLVED (Next Step 0b): disclosed composites with concrete specifics still read as authentic; the real risks are disclosure placement (up front, not a legal-disclaimer opener) and phrasing variation (folded into Next Step 6). Not validated with live engagement data — revisit once real posts are live.
- LinkedIn account-standing risk is a separate question from API scope approval: `w_member_social` access being self-serve (Constraints) doesn't clear whether LinkedIn's platform policy against automated/inauthentic posting behavior puts the account itself at risk of restriction. Research this specifically before the poster ships. (plan-eng-review, outside voice tension 4)
- No infrastructure monitoring exists for the pipeline itself (NATS/JetStream broker down, disk full, approval bot host offline) — only content failures (bad post) have a kill-switch. Add a minimal daily heartbeat check that alerts if no post/heartbeat happened in 24-36h, so a silent total outage doesn't undermine the "never misses a news cycle" promise. (plan-eng-review, outside voice tension 5) **This risk is no longer hypothetical**: the local NATS server went down silently between build sessions (2026-09-01) — the classifier's first live run failed with a wall of connection-retry errors before anyone noticed. Server was restarted manually and JetStream data survived (persisted to disk, as designed), but nothing would have caught this automatically in production. Raises this from "worth doing" to a real Next Step before this pipeline runs unattended.
- Message schema is subject names only so far (`news.raw`, `news.scored`, etc.) — now informed by Philstar RSS's actual fields (title, short description, link, pubDate, author, guid) plus a dedup key; still needs to be formally written out as a payload spec in Next Step 3.

## Success Criteria

- End-to-end pipeline runs on a schedule, producing at least one storytelling-format post per day without manual drafting effort (cadence adjustable once real throughput is known — daily is the starting target, not a hard constraint).
- Every post naming a specific official/company or asserting a factual corruption claim is held for human review before publishing — zero unreviewed risky posts.
- No fabricated "I personally experienced this" claims presented as genuine — every story is a disclosed composite archetype (never a real person's account, per Premise 2). Verified via periodic human spot-check of published posts against the drafter's source material (not just a one-time prompt review), since a drafting model can drift over time.
- NATS subjects/services are independently testable (each stage can be run and verified in isolation).

## Distribution Plan

Not applicable in the packaging sense — the only "distribution" is posting to the user's own LinkedIn account via the LinkedIn API (`w_member_social`, self-serve, no approval wait). No CI/CD/release pipeline needed for external users.

## Next Steps

0a. ✅ DONE (2026-09-01): tagged 9 real PH corporate-worker news stories from the past ~4 weeks against Premise 4's gate. Result: 0-2 of 9 (0-22%) would clearly trigger it, and even those 2 are borderline (safe if the drafter avoids naming officials/companies when not essential) rather than forced by the story itself. Conclusion: the gate-collision risk is a drafter-prompt-discipline problem, not a structural collision with the automation premise — see Next Step 6/T8, now updated to require the drafter default to NOT naming specific officials/companies unless essential to the point. Full tagged list and sources kept in session history.
0b. ✅ DONE (2026-09-01): drafted 4 sample disclosed-composite posts in the intended style. Conclusion: the authenticity paradox is real but overstated — LinkedIn's algorithm rewards specificity/first-person markers, and a disclosed composite with concrete details (peso amounts, EDSA, an electric bill) still delivers that signal. Two things that matter more than "disclosed vs. not": (1) disclosure placement — naming it up front ("Meet 'Jana' — composite...") preserves the hook, unlike a legal-disclaimer-style opener would; (2) disclosure phrasing must VARY across posts, or the disclosure line itself becomes a templated tell — the exact "generic AI content" pattern the algorithm penalizes. This was not caught until drafting real examples — added as a new drafter requirement below. Not validated with live LinkedIn engagement data (no API access yet); this is a drafted gut-check, treat as directional.
1. ✅ DONE (2026-09-01): LinkedIn developer app created (scopes: `openid`, `profile`, `w_member_social`, `email`; redirect `http://localhost:3000/callback`) and the OAuth 2.0 authorization-code flow completed — confirmed instant/self-serve as expected, no review wait. Access token obtained and stored locally (`.linkedin_token.json`, gitignored, ~59-day expiry). Implementation: `auth/linkedin_oauth.py` (Python, chosen as the project runtime), credentials in `.env` (gitignored; `.env.example` is the template) per the secrets-management constraint. Still open: researching LinkedIn's platform policy on automated/inauthentic posting behavior specifically (account-standing risk, separate from the API scope question just resolved) — see Open Questions. (plan-eng-review, outside voice tension 4)
2. ✅ DONE (2026-09-01): source selected — **Philstar.com's `/rss/nation` and `/rss/business` RSS feeds**. Checked 6 candidates against real robots.txt/ToS/freshness evidence: DOLE is unreachable (Cloudflare bot-challenge blocks even `robots.txt`); PSA has the cleanest explicit reuse permission (`Content-Signal: search=yes,ai-train=no,use=reference`) but publishes periodic statistics, not a daily news stream; Inquirer explicitly disallows `GPTBot` in robots.txt; Manila Times has a 600s crawl-delay and no working RSS feed; GMA News has the best explicit written license found ("Excerpts and links may be used, provided that full and clear credit is given...") but every category feed checked (`news`, `money`, `publicaffairs`) is stale (3-8 weeks between updates) — unusable for a daily-cadence pipeline. Philstar's feeds are live (multiple updates/day, verified same-day) and its robots.txt doesn't block general content or flag AI crawlers, though no explicit excerpt-reuse statement was confirmed (generic "All Rights Reserved"; the detailed copyright-notice page is JS-rendered and couldn't be checked further). Mitigation, now a hard scraper constraint: consume only the RSS `<title>`, short `<description>` excerpt, `<link>`, and `<pubDate>` — never fetch or reproduce the full article body — and the drafter must always write original composite-story commentary with a link-back, never reproduce Philstar's sentences. This is the same discipline already required for the libel/authenticity reasons in Constraints, now doing double duty for copyright risk too.
3. ✅ DONE (2026-09-01): local NATS server running with JetStream enabled (`nats/nats-server.conf`, port 4222, monitoring on 8222, store under `nats/data/jetstream/` — gitignored). Two JetStream streams created (`nats/setup_streams.py`, idempotent): `NEWS` (`news.raw`, `news.raw.failed`, `news.scored`) and `POSTS` (`post.drafted`, `post.pending_review`, `post.approved`), both LIMITS retention at 90 days for the audit trail Constraints calls for. Payload schema frozen as Pydantic models in `schema/messages.py` — every future service imports from there rather than redefining shapes. Dedup key (`compute_news_id`) is a hash of the article link ALONE, not link+pubDate as originally suggested — a source-side timestamp correction on the same URL would otherwise mint a new id for the same article, defeating the whole point of the key. End-to-end proven, not just designed: `nats/smoke_test.py` publishes a real `NewsRaw` message, reads it back via a JetStream pull consumer, and verifies the round trip (`SMOKE_TEST_OK`).
4. ✅ DONE (2026-09-01): scraper built (`scraper/philstar_scraper.py`) against both chosen feeds. Retry/backoff (3 attempts, exponential 2s/4s/8s) on transient fetch failures; on exhaustion, publishes `NewsRawFailed` to `news.raw.failed` per-feed rather than failing silently. New during implementation, not originally in the doc: the scraper now dedups against its OWN prior runs using a NATS JetStream KV bucket (`seen_news`) — without this, a periodically-re-run scraper would republish the same still-trending story every run, since RSS feeds keep showing recent items until they scroll off. This is the same "check a store before acting" pattern the poster will need for its own dedup-key check (Next Step 8/10) — the poster should reuse this exact KV pattern (a separate bucket, e.g. `posted_news`) rather than inventing a different mechanism, now that one is proven working. Tested: 10 unit tests (`scraper/test_philstar_scraper.py`, pure-function RSS parsing + mocked retry/backoff, no network/NATS needed) plus a live run against the real feeds and real NATS server — 30 items published on first run, 0 new (all correctly skipped as already-seen) on an immediate re-run, confirmed via JetStream's own message count (no duplicates landed in the stream).
5. ✅ DONE (2026-09-01): scorer built (`scorer/heuristic_scorer.py`) — a durable pull consumer on `news.raw`, scoring each candidate with a word-boundary keyword match (weighted, covers the labor/BPO/wage/RTO/contractualization terms from Premise 1) times a recency-decay factor (floor 0.2 after 48h so nothing hits exactly zero from age alone, but zero keyword relevance still excludes regardless of recency — a very fresh weather story is still not a corporate-worker story). Publishes only the top `min(3, relevant_count)` to `news.scored`; acks every fetched message either way so nothing gets rescored twice. Resolves the open question of what the scorer actually scores for. One real bug caught before it shipped: the module was originally named `scorer/scorer.py`, which collides with the `scorer/` package directory itself under Python's import resolution (pytest's rootless test collection puts `scorer/` on `sys.path`, so `scorer.py` inside it gets imported as the top-level `scorer` module, shadowing the package) — renamed to `scorer/heuristic_scorer.py`, no such collision. Tested: 8 unit tests (`scorer/test_scorer.py`) covering keyword matching, two explicit word-boundary false-positive cases ("Alberto" must not match "rto", "condole" must not match "dole"), recency decay, and the recency floor. Live run against the 31 real items already in `news.raw` (30 from the scraper's first run plus 1 earlier smoke-test message): only 1 cleared the relevance bar ("POCB, DMW partner to expand construction industry prospects", matched "workers"). Real, not manufactured — Philstar's nation/business feeds simply aren't heavily labor-specific on any given day, which matches the Next Step 0a finding that on-topic stories are the minority of the news cycle, not the majority. Worth watching as the pipeline runs over more days before concluding the keyword list needs expansion.
6. ✅ DONE (2026-09-01): LLM decided — Google Gemini, model `gemini-3.5-flash` (free tier; switched from an initial Anthropic Claude decision at the user's request mid-build, once a Gemini key was available). Drafter built (`drafter/post_drafter.py`) using `client.models.generate_content()` with a Pydantic `response_schema` (`DraftOutput{draft_text, archetype_disclosure}`, `response_mime_type="application/json"`) — this IS the structured-output enforcement outside voice finding 5 called for: the disclosure is a separate, schema-validated field, not something hoped-for in free prose. Reads `response.parsed` when the SDK auto-populates it, falls back to `DraftOutput.model_validate_json(response.text)` otherwise. System prompt encodes both Next Step 0a/0b requirements: worker-first framing (rule 3: only name an official/company when the post is meaningless without it) and varied disclosure phrasing (rule 2 + a code-level check — `is_duplicate_disclosure` compares the new disclosure against the last 10, stored in a NATS KV bucket `drafter_state`, same KV-store pattern as the scraper's `seen_news`; one bounded retry on an exact repeat, then accepts rather than looping forever). `GEMINI_API_KEY` added to `.env`/`.env.example` (gitignored), same secrets pattern as LinkedIn — caught a real mismatch during setup where the user's key had landed under the stale `ANTHROPIC_API_KEY=` name with no `GEMINI_API_KEY=` line at all; confirmed with the user before renaming rather than guessing. Tested: 13 unit tests (`drafter/test_drafter.py`) covering duplicate detection, prompt construction, the retry-then-accept path, the `.parsed`-vs-`.text` fallback, and non-STOP finish-reason handling (Gemini's equivalent of Claude's refusal) — all mocked, no real API call. Two real bugs caught building this: (1) same naming-collision class as the scorer — `drafter/drafter.py` collided with the `drafter/` package directory; renamed to `drafter/post_drafter.py`. (2) A genuinely serious one — see the new Constraints entry below on Windows/UTF-8. Verified end-to-end with a real Gemini call producing a real disclosed-composite post (see What already exists / Constraints).
7. ✅ DONE (2026-09-01): drafter and risk-classifier both built. Risk-classifier (`classifier/risk_classifier.py`) publishes directly to `post.approved`/`post.pending_review` — no separate router. **Gate scope clarified with the user** (Premise 4's literal wording is an OR — "naming a specific official/company" — which would gate almost everything, including our own first neutral draft naming two government agencies): narrowed to route to review only when the post (a) names a specific INDIVIDUAL with an attributed claim, (b) names a specific PRIVATE COMPANY with a negative claim, or (c) asserts any corruption/wrongdoing claim at all. A neutral government-agency mention (DOLE, POCB, DMW) in a positive/factual context is NOT gated — matches the actual libel rationale in Constraints (identifiable people/companies), not "any name at all." **Defense in depth**: a deterministic keyword backstop (corrupt, bribery, fraud, embezzle, anomalous, illegal, scam, graft, ...) runs alongside the Gemini LLM verdict and can only push toward "risky", never override toward "safe" — directly answers the Open Questions' "what happens on a false negative" concern for the most unambiguous cases, same "cheap heuristic first" pattern as the scorer. Tested: 11 unit tests (`classifier/test_risk_classifier.py`) covering backstop word-boundary correctness, the backstop-overrides-LLM-safe-verdict case, and Gemini's non-STOP finish-reason handling. **Verified live, both paths**: the real "POCB, DMW partner..." draft correctly routed to `post.approved` (neutral agency mention); a deliberately risky manufactured example (a named official accused of bribery) correctly routed to `post.pending_review`, caught by both the backstop keyword AND the LLM's own reasoning independently.
8. ✅ DONE (2026-09-01): held-out eval sets built for both the classifier and the drafter. **Contamination caveat honestly not resolved** — written by the same person who built the classifier (a solo project has no one else to write it); mitigated by re-deriving examples from Premise 4's text and the narrow-interpretation decision rather than the classifier's own prompt wording, documented as a known limitation in the eval set file itself. Still an early-warning smoke test at n=18, not a statistical guarantee — Premise 4's review gate remains the real backstop.

   **Classifier eval** (`eval/classifier_eval_set.json`, `eval/run_classifier_eval.py`, baseline in `eval/results/classifier-eval-baseline.json`): 18 synthetic-but-realistic examples specifically constructed to stress-test the decision boundary — 9 safe (including the tricky ones: a positively-framed private company mention, a story deliberately drafted without naming an official who could have been named, corruption-adjacent-sounding-but-actually-neutral institutional mentions) and 9 risky (including cases with no backstop-keyword to lean on, pure LLM judgment calls like a named official's inflammatory quote with no corruption language at all). **Result: 18/18 correct, 0 false negatives, 0 false positives.**

   **A real, unplanned finding along the way**: `gemini-3.5-flash`'s free tier turned out to be capped at 20 requests/DAY (not just per-minute) — discovered when the eval run failed partway through with `RESOURCE_EXHAUSTED`, quota metric `GenerateRequestsPerDayPerProjectPerModel-FreeTier`, limit 20. Real production steady-state (2-6 calls/day) would technically fit, but left zero headroom for retries or testing — today's dev/testing alone exhausted it. **Switched the drafter and classifier to `gemini-3.5-flash-lite`** (confirmed as a real, current model ID via `client.models.list()`, not guessed) for a much higher daily quota, verified by re-running the full 18-example eval afterward with zero rate-limit errors (vs. 12 errors on the first attempt with the non-lite model).

   **Drafter adherence check** (`eval/run_drafter_eval.py`, 5 synthetic items, lite model): 4/5 drafts had the `archetype_disclosure` field appear verbatim inside `draft_text` as system prompt rule 6 requires; 1/5 had the model paraphrase slightly differently between the two fields despite the instruction. A targeted re-run of the same scenario produced a correctly-matching pair, suggesting this is LLM stochastic variance on structured-field adherence (occasionally imperfect, not systematically broken) rather than a code defect — worth a periodic spot-check once live, not a blocker. 0/5 repeated a prior disclosure verbatim across the run (varied-phrasing requirement holding up).
9. ✅ DONE (2026-09-01), fully verified live: approval bot built (`approval_bot/telegram_approval_bot.py`), plus — pulled forward from Next Step 10 at the user's request — the poster's dedup-key gate (`poster/dedup_store.py`) and the kill-switch/incident-response mechanism (`approval_bot/kill_switch.py`).

   **Architecture note**: this is the first stage that ISN'T a periodic batch script like every prior stage. It's reactive/always-on — a pending item can sit for hours or days per the "no approval timeout" decision, and a human can click Approve/Reject at any moment. Concretely this meant: (a) the underlying NATS message is acked as soon as it's delivered to Telegram, not held open while waiting on a human — JetStream's ack-wait is far too short for human-timescale review, and holding it open risks redelivery storms; the pending item's content instead lives in a NATS KV bucket (`pending_approvals`) that the approve/reject decision operates against; (b) `python-telegram-bot` (22.8) is run via its manual async lifecycle (`initialize()` / `start()` / `updater.start_polling()`) rather than the blocking `run_polling()` helper, so it can share the event loop with our own NATS connection — verified against the library's own docs rather than guessed, given how much SDK API drift has bitten this build already (Gemini's structured-output shape, its real free-tier quota).

   **Dedup-key gate** (`poster/dedup_store.py`): `already_posted`/`mark_posted` against a `posted_news` KV bucket — same pattern as the scraper's `seen_news` and the drafter's `drafter_state`. Built now, ready for the actual poster (Next Step 10) to call before every LinkedIn API request.

   **Kill-switch / incident response** (`approval_bot/kill_switch.py`): there's no automated way to detect after the fact that a *published* post was actually risky — that's exactly the false-negative scenario this exists for, and the classifier eval couldn't fully rule it out (n=18, not a guarantee). So the trigger is a human flag: a new `/flag <news_id> [reason]` Telegram command increments a counter; 2+ flags within a rolling 24h window pauses auto-posting (a `poster_paused` KV flag the future poster must check) and alerts the user with next steps (check LinkedIn, delete/edit, note what went wrong). Added `/unpause` as the necessary complement — a pause mechanism with no way to resume isn't a complete feature — and `/status` to check current pause state.

   Tested: 10 unit tests for the kill-switch (including the exact 24h-window boundary and the threshold-of-2 behavior), 3 for the dedup store, 14 for the approval bot's business logic (including a double-approve idempotency check — clicking Approve twice must not publish twice). New `conftest.py` with a `FakeKV`/`FakeJS` test double, reused across all three. First async test code in this project — added `pytest-asyncio` (which needed pytest bumped to 9.1.1) and `pytest.ini` (`asyncio_mode = auto`).

   **Live testing surfaced four real bugs, none of which the unit tests could have caught** (they mock Telegram/NATS; these were integration-level):
   1. **Windows background-process detachment.** Starting long-running processes (NATS, the bot) via Bash's `&`/`disown` did not reliably survive between tool-call turns — NATS silently died at least twice mid-session, and a stale bot process from a dead-NATS run kept retrying invisibly in the background, later colliding with a fresh instance's JetStream durable-consumer binding (`consumer is already bound to a subscription`). Switched to PowerShell's `Start-Process` (a genuinely detached Windows process, not a job-control-emulated background job under Git Bash/MSYS) for both. Worth remembering for any future long-running process on this machine, not just this project.
   2. **Async lifecycle order.** Manual PTB startup was `initialize() → start() → updater.start_polling()`; the documented order is `start_polling()` BEFORE `start()`. With the wrong order, PTB's polling loop successfully fetched updates from Telegram (confirmed via direct `getUpdates` calls — the queue drained) but never dispatched them to any handler, with zero trace anywhere despite configured logging and a registered error handler. Symptom looked exactly like a silent handler crash; root cause was unrelated to handler code entirely.
   3. **A redundant, actively-harmful "catch-up sweep."** The original design used a second, throwaway JetStream consumer (`-catchup` suffix) to deliver anything that arrived while the bot was offline. This was unnecessary — a durable push consumer with the SAME name already resumes from its last position across restarts, that's the entire point of "durable" — and the throwaway consumer's default deliver policy (ALL) redelivered the ENTIRE `post.pending_review` history on every single restart. Confirmed live: it re-sent an already-approved test post to Telegram after a routine restart. Removed the mechanism entirely rather than patched.
   4. **Diagnostic tooling interfered with the system under test.** Telegram allows only one active `getUpdates` poller per bot token. Running a raw diagnostic `getUpdates` call (to check what the bot had received) WHILE the bot's own polling was live triggered a `Conflict` error that permanently killed the bot's polling loop (no auto-recovery observed) — turning a working bot into a silently broken one, and making the debugging session look like it had found new bugs when it had actually just broken the thing it was checking. Lesson: never call a long-polling API's own polling endpoint from a second process while a live instance is running it.

   All four fixed, then a complete live pass confirmed correct: a real risky draft delivered to Telegram → Approve clicked → landed in `post.approved` with `reviewed_by=human`, verified via direct JetStream/KV inspection; `/flag` recorded correctly and, on the second flag within 24h (one manual + one via the bot), correctly triggered the kill-switch pause message; `poster_paused` confirmed `True` in KV; `/unpause` correctly cleared it back to `False`; `/status` correctly reported pause state throughout.

   **Still open**: real Telegram credentials (`TELEGRAM_BOT_TOKEN` via @BotFather, `TELEGRAM_CHAT_ID` via the new `approval_bot/get_chat_id.py` helper) needed before a live end-to-end test of button clicks and commands.
10. ✅ DONE (2026-09-01), verified with a real live LinkedIn post: poster built (`poster/linkedin_poster.py`) using the LinkedIn Posts API (`POST /rest/posts`, verified against current LinkedIn docs rather than assumed — the API has moved from the older `ugcPosts` endpoint). Author URN resolved via `GET /v2/userinfo`'s `sub` claim (`urn:li:person:{sub}`), using the OIDC scopes already granted in Next Step 1. Retries LinkedIn's own documented transient status codes (429/500/503, 3 attempts, exponential backoff); non-retryable errors (401/403/etc.) fail immediately rather than wasting retries on something a retry can't fix. Both gates from Next Step 9 wired in: kill-switch checked first (does nothing if paused, logs clearly), dedup-key checked per item (already-posted items are acked and skipped, not re-posted). 11 unit tests, all mocked — no real API calls in the test suite.

    **A real near-miss, caught before it happened:** the `post.approved` queue still had 3 items sitting in it from earlier testing — 1 genuine ("Ramon"/POCB-DMW post) and 2 duplicates of the FABRICATED bribery-accusation test content from Next Step 9's live verification (a made-up claim against a fictional "Secretary Juan Dela Cruz"). Running the poster as-is would have published that fake corruption claim to a real, public LinkedIn profile. Caught by inspecting the queue before running the poster live, confirmed with the user, and the 2 fake items were deleted directly from the JetStream stream (`js.delete_msg`) before proceeding — this required understanding that the POSTS stream's LIMITS retention means a message persists independently of any one consumer's ack state, so acking via a throwaway inspection consumer would NOT have removed them from what the poster's own consumer could see; direct stream deletion was the only correct fix.

    **Explicit confirmation gate**: this is the first action in the whole build with a real, public, hard-to-reverse consequence — everything before this posted to the user's own private Telegram chat or called free-tier/sandboxed APIs. Full post text was shown to the user before running the poster live, and explicit go-ahead was obtained.

    **Live result**: `[posted] news_id=555883236b6cfa98 -> urn:li:share:7500520154842247169`. Verification quirk: `GET /rest/posts/{urn}` returned 403 (needs the restricted `r_member_social` scope we don't have — expected, not a failure signal) and the documented `https://www.linkedin.com/feed/update/urn:li:ugcPost:<id>/` URL pattern didn't resolve for this `share`-prefixed URN either. The reliable confirmation was the user's own LinkedIn profile/activity page, where the post is genuinely live. **The pipeline is now complete end-to-end**, Next Steps 1-10 all done: a real Philstar news item was scraped, scored, drafted by Gemini, classified safe, and published to LinkedIn, with a human-supervised catch along the way preventing fabricated test content from going out under the same automation.
11. Add a minimal daily heartbeat check (e.g. a cron ping or uptime monitor) that alerts if no post/heartbeat happened in 24-36h, covering infrastructure failures (broker down, disk full, bot host offline) that the content-focused kill-switch doesn't catch. (plan-eng-review, outside voice tension 5)
12. ✅ DONE (2026-09-01): scheduling set up per the user's explicit choice (automatic, weekdays, 8am Philippine Time, until told to stop). Orchestrator built (`scheduler/run_pipeline.py`) — runs scraper → scorer → drafter → classifier → poster in sequence as subprocesses, each stage running regardless of earlier failures, `PYTHONUTF8=1` set explicitly per subprocess. Every run logged to `scheduler/logs/` (gitignored). Verified live end-to-end before any automation was registered: found 4 genuinely new scraped items, 0 scored relevant, and — the important check — the one leftover already-posted item in the queue was correctly NOT redelivered/reposted (durable consumer position + the dedup-key gate both hold, confirmed directly against the KV store before trusting it).

    **Always-on pieces** (NATS, the approval bot — deliberately excluded from the orchestrator, since they're persistent services, not periodic jobs): registered via the per-user Startup folder (`shell:startup`), not Task Scheduler — this session's permissions turned out not to include Task Scheduler registration rights (`Access is denied` from both `Register-ScheduledTask` and `schtasks.exe`), confirmed as a genuine rights issue, not something to route around cleverly. The Startup folder is user-writable without elevation and serves the same "at login" trigger the user asked for. **A real bug caught before trusting it**: the first version of the VBS launcher resolved its target `.ps1` path relative to the VBS file's OWN location (the Startup folder), but only the VBS itself was copied there — the actual `.ps1` only exists in the project's `scheduler/` directory. Silent failure (no processes started, no visible error) until fixed to use an absolute path. Verified working end-to-end by simulating a fresh login (killed everything, ran the deployed Startup-folder copy, confirmed NATS + bot both started clean).

    **Later correction (2026-09-03)**: the Task Scheduler registration itself was re-attempted from a Claude Code session on a later day and succeeded (`Register-ScheduledTask` no longer denied) — the earlier "Access is denied" was evidently session-specific, not a durable machine-wide restriction. The recurring `NatsLinkedInAutomation-Pipeline` task is now actually registered. One real incident this exposed: the very first scheduled run published an approved post but the poster crashed before publishing it (see Next Step 13's image-attachment bug below) — the fix was to make image generation truly best-effort (catch everything, never let it block a real post), not to touch the scheduling mechanism itself.

13. ✅ DONE (2026-09-03): post images added, after two false starts on the image source. First attempt — Gemini's image models (`gemini-3.1-flash-lite-image`) — confirmed via Google's own pricing docs to have NO free tier at all for image generation (unlike the text models used elsewhere), and even after the user approved the small paid cost (~$0.03-0.07/image), the project's Google AI Studio billing was never actually enabled, so every attempt returned `429 RESOURCE_EXHAUSTED` (`limit: 0` on the free tier). **A real production incident came from this**: the first automated run after the image code shipped drafted and auto-approved a real post, but the poster crashed outright on that 429 instead of falling back to text-only, because `post_one`'s exception handling only caught `(ImageGenerationError, requests.RequestException)` — not the actual `google.genai.errors.ClientError` the SDK raises. The approved post silently never went out. Fixed by broadening to a deliberate bare `except Exception` around the whole image-generation-and-upload block, documented in the code as intentional: a real post publishing matters far more than enumerating every exception type an image API can raise. Second attempt — Hugging Face's Inference Providers — looked free but their actual pricing docs show only $0.10/month in free credits for non-PRO accounts, nowhere near enough for daily posts; dropped before writing any code once this was found. **Final, working choice**: Unsplash's free "Demo" tier (`imagegen/unsplash_photo_picker.py`) — real stock photos via keyword search (a small hand-picked theme-keyword table, e.g. "jeepney" → "Manila jeepney commute", falling back to a generic "Philippines office worker" query), 50 req/hr free with no card, no generation cost. Unsplash's API Guidelines require both a "trigger a download" ping on actual use and photographer+Unsplash attribution with a UTM-tagged profile link — both implemented; the attribution line is appended to the post's `commentary` text itself, since LinkedIn's Posts API has no separate image-caption field. Photo attachment is best-effort exactly like the Gemini path was meant to be: any failure (no results, network error, LinkedIn image-upload error) falls back to a text-only post rather than blocking it. Verified live end-to-end, including an intentionally photo-relevant query ("jeepney home after a long shift") that returned a genuinely on-topic Manila jeepney street photo.

14. ✅ DONE (2026-09-03): scraper widened from Philstar-only to three sources, at the user's request ("i want also to consider other sources"), with the same ToS/robots.txt diligence as the original Next Step 2 — not just picking obvious PH outlets. **Rappler was checked and deliberately excluded**: its robots.txt disallows `anthropic-ai` and `ClaudeBot` by name (distinct from its general crawl rules), a direct signal that this kind of AI use isn't welcome there — respected rather than worked around with a different User-Agent string. **GMA News re-checked and added**: the earlier Next Step 2 pass had found GMA's feeds stale and ruled it out, but that check hit different (broader-category) feed URLs; the actual per-section feeds (`data.gmanetwork.com/gno/rss/news/nation/feed.xml`, `.../money/economy/feed.xml`, found via GMA's own RSS index page) update multiple times a day, and GMA's robots.txt explicitly allows ClaudeBot/anthropic-ai site-wide. **Inquirer added with a real restriction honored**: their User Agreement limits external reuse to headlines only (no excerpt/description text) without separate written syndication clearance — narrower than Philstar/GMA — so the scraper publishes `excerpt=""` for every Inquirer item, enforced via a per-feed `include_excerpt` flag rather than trusting each call site to remember. Scraper renamed `scraper/philstar_scraper.py` → `scraper/news_scraper.py` to match; `schema/messages.py`'s `source` field widened to `Literal["philstar", "gma", "inquirer"]`. **A real bug caught on the first live run**: Inquirer's business feed sends a stray space+tab before its `<?xml` declaration, which `xml.etree.ElementTree` rejects outright (declaration must be the very first thing) — this crashed the entire scraper run, not just that one feed, since the parse step wasn't inside the same per-feed try/except already used for fetch failures. Fixed two ways: `xml_text.lstrip()` before parsing (handles the actual cause), and wrapping the parse step in its own try/except publishing `NewsRawFailed` per-feed (so any future malformed feed from any source degrades the same way a fetch failure already did, rather than taking down the whole run). Verified live: all 6 feeds across 3 sources scraped cleanly, 100 candidates considered by the scorer in one run (up from Philstar-only's typical 20-30/day).

    **Recurring weekday trigger** (the actual scraper→poster pipeline, 8am Mon-Fri): confirmed this machine's timezone (Singapore Standard Time, UTC+8) already matches Philippine Time exactly — no DST on either side — so a plain 8:00 AM local-time trigger needs no conversion. Registration itself is blocked by the same Task Scheduler permission wall, so `scheduler/register_scheduled_tasks.ps1` is prepared and ready but **must be run once by the user in an elevated PowerShell window** — this session cannot do it. The script includes its own verification command and an unregister command for when the user wants to stop it.

    **Known dependency**: the 8am pipeline run requires NATS/the bot to already be running, which (via the Startup-folder approach) only happens if the user has actually logged into Windows. If not logged in at 8am, that day's run fails cleanly at the scraper stage (can't reach NATS) rather than silently — connects to the still-open infra-heartbeat Next Step above.

## Reviewer Concerns

Resolved during /plan-eng-review: the Data Privacy Act 2012 compliance question was removed by restricting Premise 2 to composite archetypes only (no real-person stories); the incident-response and kill-switch gaps were elevated to blocking Open Questions/Next Steps to match the risk-classifier reliability gate.

## NOT in scope

- Cross-posting to platforms other than LinkedIn (Twitter/X, Facebook) — Premise 6 scopes this to LinkedIn only; the NATS architecture leaves room to add later.
- Scraping more than one news source before the first is proven (Next Step 3) — deferred to keep the initial build small.
- Auto-expiry/escalation for pending-approval posts — explicitly rejected (test gap 2); posts stay pending indefinitely by design, not by omission.
- A dedicated secrets manager (Vault/1Password CLI) — gitignored `.env` env vars are enough for a solo pipeline (architecture issue 1); revisit only if this grows beyond one operator.
- A separate router service between the risk-classifier and the approval/poster subjects — folded into the classifier itself (architecture issue 2); nothing in this design calls for routing logic to evolve independently.
- Sourcing "real anonymized" worker stories from actual people — dropped entirely, not deferred (code quality issue 2); composite archetypes cover the authenticity goal without a Data Privacy Act 2012 compliance question.

## What already exists

Nothing to reuse — this is a greenfield repo (`README.md` and `CLAUDE.md` only, no application code). No existing flows, scrapers, or LLM-calling code in this repository.

## Failure Modes

| Codepath | Realistic failure | Test? | Error handling? | User-visible? |
|---|---|---|---|---|
| Scraper | Source API down/rate-limited | ✅ Built: `scraper/test_philstar_scraper.py` (10 unit tests) | ✅ Built: retry/backoff + `news.raw.failed` signal | Visible (distinct from zero-news day) — **was CRITICAL GAP, now CLOSED** |
| Scorer | No candidate scores highly enough (zero-news day) | Not yet | Tracked as an open question | Ambiguous until resolved — not critical (tracked, low likelihood) |
| Drafter | Model drifts into a fabricated first-person claim | Held-out eval (Next Step 6) + periodic spot-check | Prompt constraint (Open Questions) | Caught by eval/spot-check, not silent |
| Risk-classifier | False negative — risky post slips through | Held-out eval (Next Step 6) | Kill-switch + incident-response (both now blocking) | Caught by kill-switch, not silent |
| Poster | Duplicate post on JetStream redelivery | Not yet (unit test needed at build time) | Dedup-key check (now blocking Next Step) | Prevented before it's user-visible |
| Approval bot | Bot offline / user doesn't check it | N/A — intentional | Post stays safely pending forever | Visible only as a growing queue, by design |

## Worktree Parallelization Strategy

**Sequential prerequisites — ✅ both cleared (2026-09-01):** (1) news source picked (Philstar RSS, Next Step 2); (2) NATS subject schema frozen and proven end-to-end (`schema/messages.py`, Next Step 3). Lanes B-F below are now unblocked and can proceed in parallel worktrees against that frozen contract.

| Step | Modules touched | Depends on |
|------|----------------|------------|
| News source selection | (design only, no code) | — |
| Subject schema | (design only, no code) | News source selection |
| Scraper | `scraper/` | Subject schema |
| Scorer | `scorer/` | Subject schema |
| Drafter + risk-classifier + eval set | `drafter/`, `classifier/`, `eval/` | Subject schema |
| Approval bot | `approval-bot/` | Subject schema |
| Poster (+ dedup store) | `poster/` | Subject schema |

Lane A (sequential, blocks all others): finalize subject schema.
Lane B (parallel, independent): scraper.
Lane C (parallel, independent): scorer.
Lane D (parallel, independent): drafter + risk-classifier + eval-set creation (the eval examples themselves need no code and can start immediately, even during Lane A).
Lane E (parallel, independent): approval bot.
Lane F (parallel, independent): poster + dedup store.

Execution order: finish Lane A first (or at least freeze the schema), then launch B through F in parallel worktrees — each is independently testable against the frozen schema with mocked NATS pub/sub, matching the design's own stated goal of per-stage independence. Integrate all five against a real local NATS server once each lane's unit tests pass. No two lanes touch the same module directory, so there's no merge-conflict risk between them.

## Implementation Tasks

Synthesized from this review's findings. Each task derives from a specific finding above. Run with Claude Code or Codex; checkbox as you ship.

- [x] **T1 (P1)** — scraper — Add retry/backoff and a `news.raw.failed` signal distinct from a zero-news day
  - Surfaced by: Failure-mode synthesis — critical gap (no test, no error handling, silent failure)
  - Result: `scraper/philstar_scraper.py` — verified via mocked-failure unit tests (`test_exhausts_retries_and_raises`, `test_http_error_status_triggers_retry_path`) and a live successful run
- [ ] **T2 (P1, human: ~10min / CC: ~2min)** — constraints — Document secrets (LinkedIn token, Telegram token, LLM key) as env vars via gitignored `.env`, never in NATS payloads/logs/config
  - Surfaced by: Architecture issue 1
  - Files: `.env.example`, `.gitignore`
  - Verify: grep NATS payload schemas and log statements for accidental secret fields
- [ ] **T3 (P2, human: ~1h / CC: ~15min)** — risk-classifier — Publish directly to `post.approved`/`post.pending_review`; no separate router
  - Surfaced by: Architecture issue 2
  - Files: `classifier/`
  - Verify: unit test both branches of the classifier's publish decision
- [x] **T4 (P1)** — poster — Define dedup key in payload; poster checks a local posted-IDs store before posting
  - Surfaced by: Architecture issue 3 (JetStream at-least-once double-post risk)
  - Result: `poster/dedup_store.py` (`already_posted`/`mark_posted`, `posted_news` KV bucket), 3 unit tests. Dedup key itself (`news_id`) has been in the schema since Next Step 3; this closes the "check before acting" gate. Actual "replay twice, confirm one LinkedIn post" verification deferred to Next Step 10 once the poster calls the real API.
- [x] **T5 (P1)** — kill-switch/incident-response — Define kill-switch threshold (2 risky posts flagged within 24h → pause poster + alert) and a documented retraction process
  - Surfaced by: Code quality issue 1 (elevated from Reviewer Concerns)
  - Result: `approval_bot/kill_switch.py` + the bot's `/flag`, `/unpause`, `/status` commands, 10 unit tests. Human-flagged rather than automated, since there's no automated way to detect a published post was actually risky after the fact — that IS the false-negative scenario this guards against.
  - Verify: simulate 2 misses in 24h and confirm the poster subscription pauses
- [x] **T6 (P1)** — eval — Build a held-out set of 15-20 labeled news items (safe/risky mix); measure risk-classifier false-negative rate and drafter archetype-disclosure adherence
  - Result: 18/18 classifier accuracy, 0 false negatives, 0 false positives (baseline: `eval/results/classifier-eval-baseline.json`). Drafter: 4/5 exact disclosure-field adherence. Contamination caveat not fully resolved (solo project) — documented as a known limitation, not hidden.
  - Surfaced by: Test gap 1; refined by outside voice findings 6 and 7
  - Files: `eval/`
  - Verify: run the eval set, record false-negative rate as a checked-in baseline
- [ ] **T7 (P2, human: ~1h / CC: ~15min)** — scorer — Implement as a cheap heuristic/keyword pass (no LLM), rank all candidates, pass only top 1-3 to drafter+classifier
  - Surfaced by: Performance issue 1
  - Files: `scorer/`
  - Verify: feed 30+ mock candidates, confirm only top-N reach the drafter stage (assert LLM call count)
- [ ] **T8 (P1, human: ~1h / CC: ~15min)** — drafter — Decide the drafter's LLM and the concrete mechanism enforcing composite-archetype disclosure (structured output field, not a loose prompt instruction)
  - Surfaced by: Outside voice finding 5 (no owner existed for this "must resolve" item)
  - Files: `drafter/`
  - Verify: unit test that drafter output always carries the disclosure field/marker
- [x] **T9 (P1)** — scraper — Check each candidate news source's ToS/robots.txt for redistribution terms before building against it; prefer official RSS + link-back-with-excerpt over full-text reproduction
  - Surfaced by: Outside voice finding 8 (named once in Constraints, never followed up)
  - Result: Philstar `/rss/nation` + `/rss/business` selected (see Next Step 2). DOLE/Inquirer/Manila Times/GMA all ruled out by real evidence (unreachable, explicit AI-bot block, no working feed, or stale feed respectively).

- [x] **T10 (P1)** — validation — Tag a week of real trending PH labor/corporate news for review-gate trigger rate
  - Surfaced by: Outside voice tension 1
  - Result: 0-2 of 9 real stories (0-22%) would trigger the gate, and only due to drafting choices, not the news itself. See Open Questions/Next Step 6.
- [x] **T11 (P1)** — validation — Draft 3-5 disclosed-composite sample posts and gut-check/trial engagement
  - Surfaced by: Outside voice tension 2
  - Result: go, with two new drafter requirements (worker-first framing, varied disclosure phrasing) folded into Next Step 6. Directional only — no live LinkedIn engagement data yet.
- [ ] **T12 (P1, human: ~30min / CC: ~10min)** — poster — Research LinkedIn's automated/inauthentic-posting policy for account-standing risk, separate from API scope approval
  - Surfaced by: Outside voice tension 4
  - Files: none (research note, may inform posting cadence/behavior)
  - Verify: documented conclusion before Next Step 10 (poster) goes live
- [ ] **T13 (P2, human: ~1h / CC: ~15min)** — ops — Add a minimal daily heartbeat check alerting if no post/heartbeat in 24-36h
  - Surfaced by: Outside voice tension 5
  - Files: ops/monitoring script
  - Verify: kill the NATS broker in a test environment and confirm the heartbeat check fires

_No new tasks from Code Quality issue 2 (Premise 2 restriction) or cross-model tension 3 (NATS-native decision reaffirmed, no change) — scope/doc decisions, not build tasks._

## What I noticed about how you think

- When I offered a menu of "startup / intrapreneurship / hackathon / learning" framings, you skipped past all of them twice and kept steering straight to the content itself — "trending issues... relevant to corporate workers." You care more about getting the thing right than about labeling it.
- You picked "fully automated" initially, but when I laid out the specific libel and authenticity risks, you agreed to a review gate without hesitation on both premises. That's evidence-driven, not stubborn.
- The repo was already named `nats-linkedin-automation` before this session started — you'd already committed to the architecture's backbone before we even talked about approaches, and Approach B (NATS-native) is what you picked when given the choice anyway.
